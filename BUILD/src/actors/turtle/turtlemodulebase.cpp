/*
DO NOT EDIT THIS FILE!

This file is autogenerated from "--update" and will be replaced
every build time

*/

// Self includes
#include "turtlemodulebase.h"
#include "turtleplugin.h"

// Kumir includes
#include <kumir2-libs/extensionsystem/kplugin.h>

// Qt includes
#include <QtCore>
#include <QtGui>

namespace ActorTurtle {

TurtleModuleBase::TurtleModuleBase(ExtensionSystem::KPlugin* parent)
    : QObject(parent)
{
    bool hasGui = true;
#ifdef Q_OS_LINUX
    hasGui = getenv("DISPLAY")!=0;
#endif
    if (hasGui) {
        static const QString currentLocaleName = QLocale().name();
        
        
    }
}

/* protected */ const ExtensionSystem::CommandLine& TurtleModuleBase::commandLineParameters() const
{
    TurtlePlugin * plugin = qobject_cast<TurtlePlugin*>(parent());
    return plugin->commandLineParameters_;
}

/* public virtual */ void TurtleModuleBase::handleGuiReady()
{
}

/* public virtual */ QString TurtleModuleBase::initialize(const QStringList &configurationParameters, const ExtensionSystem::CommandLine & runtimeParameters)
{
    Q_UNUSED(configurationParameters);
    Q_UNUSED(runtimeParameters);

    // Return error text or an empty string on successfull  initialization
    return QString();
}

/* public virtual */ bool TurtleModuleBase::isSafeToQuit()
{
    return true;
}

/* public virtual slot */ void TurtleModuleBase::loadActorData(QIODevice * source)
{
    Q_UNUSED(source);  // By default do nothing

}



/* public */ QList<QMenu*> TurtleModuleBase::moduleMenus() const
{
    bool hasGui = true;
#ifdef Q_OS_LINUX
    hasGui = getenv("DISPLAY")!=0;
#endif
    if (hasGui) {
        QList<QMenu*> result;
        
        return result;
    }
    else {
        return QList<QMenu*>();
    }
}

/* protected */ void TurtleModuleBase::msleep(unsigned long msecs)
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    plugin->msleep(msecs);
}

/* public */ QDir TurtleModuleBase::myResourcesDir() const
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    return plugin->myResourcesDir();
}

/* public */ ExtensionSystem::SettingsPtr TurtleModuleBase::mySettings() const
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    return plugin->mySettings();
}





/* protected */ void TurtleModuleBase::setError(const QString & errorText)
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    plugin->errorText_ = errorText;
}

/* protected */ void TurtleModuleBase::sleep(unsigned long secs)
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    plugin->sleep(secs);
}

/* public virtual */ QVariantList TurtleModuleBase::templateParameters() const
{
    TurtlePlugin * plugin = qobject_cast<TurtlePlugin*>(parent());
    return plugin->defaultTemplateParameters();
}

/* protected */ void TurtleModuleBase::usleep(unsigned long usecs)
{
    TurtlePlugin* plugin = qobject_cast<TurtlePlugin*>(parent());
    plugin->usleep(usecs);
}



} // namespace ActorTurtle
