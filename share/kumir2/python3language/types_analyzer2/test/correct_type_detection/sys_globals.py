import sys as s
a = s.abiflags  # expects: str
b = s.argv  # expects: list<str>
c = s.base_exec_prefix  # expects: str
d = s.base_prefix  # expects: str
e = s.byteorder  # expects: str
f = s.builtin_module_names  # expects: (str)
g = s.copyright  # expects: str
h = s.hexversion  # expects: int
i = s.path  # expects: list<str>
j = s.platform  # expects: str
k = s.prefix  # expects: str
l = s.ps1  # expects: str
m = s.ps2  # expects: str
n = s.version  # expects: str
o = s.api_version  # expects: int